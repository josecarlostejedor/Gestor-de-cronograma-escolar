
# Este es un proyecto de Aplicación Web (React/TypeScript) con persistencia Cloud.
# No utiliza Python/Streamlit directamente, por lo que no requiere pip install.
# La persistencia se maneja vía JSONBin.io para que funcione en cualquier terminal/PC.

# Dependencias lógicas (Cargadas vía importmap/CDN en index.html):
react >= 19.0.0
react-dom >= 19.0.0
lucide-react >= 0.460.0
docx >= 8.5.0
file-saver >= 2.0.5

# Persistencia Configurada:
CloudStorage: JSONBin.io
BinID: 6986233f43b1c97be96a93ee
